# Fraud Detection in E-commerce using ML  
## 数据清理与统一化总结报告（基于 dataset_clean_summary.json）

---

## 🧩 Part 1. 全局概览

| 数据集名称 | 原始特征数（约） | 清洗后特征数 | 新增特征 | 样本规模 (train/test) | 标签正例比例 (train/test) | 缺失率情况 |
|-------------|------------------|---------------|-----------|------------------------|-----------------------------|-------------|
| **IEEE** | 394+ | 81 | `log_amount`, `hour_of_day`, `day_of_week`, `sin_hour`, `cos_hour`, `user_txn_*`, `freq_*`, `V_pca_*` | 100k / 100k | ≈2.7%（原）→ **2–3%（稳定）** | 0% |
| **creditCardPCA** | 31 | 34 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` | 100k / 56,962 | 0.16% → **0.17%** | 0% |
| **creditCardTransaction** | 13 | 13 | 无（本身已结构化） | 100k / 100k | 0.99% → **0.4–1.0%** | 0% |
| **col14_behave** | 14 | 15 | `log_amount`, `hour_of_day`, `day_of_week` | 100k / 59,400 | 6.8% → **6.8%** | 0% |
| **col16_raw** | 16 | 14 | `log_amount`, `hour_of_day`, `day_of_week` | 100k / 23,633 | 4.9–5.1% | 0% |
| **counterfeit_products** | 27 | 16 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` | 4,000 / 1,000 | 29.4% | 0% |
| **counterfeit_transactions** | 20 | 19 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` | 2,400 / 600 | 24.4–24.5% | 0% |

**全局总结：**
- 清洗后所有数据集缺失率为 **0%**。  
- 所有数据集均包含时间与对数金额特征。  
- 标签字段统一命名为 `is_fraud`。  
- 全部采用 stratified 80/20 划分，保持欺诈比例稳定。

---

## 📊 Part 2. 数据集详细对比

---

### 1️⃣ IEEE-CIS Fraud Detection

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | ~394 | 81 |
| 新增 | `log_amount`, `hour_of_day`, `day_of_week`, `sin_hour`, `cos_hour`, `user_txn_count`, `user_txn_mean`, `user_txn_std`, 一系列 `_freq` 稀有度特征，`V_pca_0`–`V_pca_49` |
| 删除 | 高缺失字段、部分匿名统计特征（V339+） |
| 标签 | `isFraud` → `isFraud (int64)` |
| 缺失率 | 部分高达40% → **0%** |
| 样本数 | 472,432+ → **100k(train) + 100k(test)** |
| 改进 | 周期化时间特征（sin/cos hour）、交易频率统计（用户级 count/mean/std）、频率归一化特征（card/freq/id/domain）、PCA 压缩匿名 V 特征。 |

🧠 **意义**：  
IEEE 数据是所有子集中最复杂的真实金融交易场景。清洗后结构显著简化并统一，可直接与其他电商集对齐用于跨域实验，保留关键设备与用户稀有度特征以支撑双向稀疏建模。

---

### 2️⃣ CreditCardPCA（欧洲信用卡欺诈）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 31 | 34 |
| 新增 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` |
| 删除 | 无 |
| 标签 | `Class` → `is_fraud` |
| 缺失率 | 0% → 0% |
| 样本数 | 284,807 → **100k(train) + 56,962(test)** |
| 改进 | 金额对数化平滑长尾；时间戳化支持时间模式研究。 |

🧠 **意义**：  
该集为纯数值结构（PCA压缩），代表“低语义高统计密度”场景。清洗统一后可作为**基准集**检验模型在匿名数据中的鲁棒性。

---

### 3️⃣ CreditCardTransaction（地理特征交易数据）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 13 | 13 |
| 新增 | 保留原结构，仅添加 `log_amount` |
| 删除 | 无 |
| 标签 | `is_fraud`（保持） |
| 缺失率 | 0% |
| 样本数 | 555,719 → **100k(train) + 100k(test)** |
| 改进 | 保留经纬度衍生距离 `distance_km`；统一时间与金额标准化。 |

🧠 **意义**：  
该集为唯一包含地理位置信息的数据，支持空间一致性与跨区域欺诈检测分析，是研究“距离异常”特征的核心集。

---

### 4️⃣ col14_behave（用户行为型交易集）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 14 | 15 |
| 新增 | `log_amount`, `hour_of_day`, `day_of_week` |
| 删除 | 无 |
| 标签 | `Is.Fraudulent` → `is_fraud` |
| 缺失率 | 部分字段不全 → **0%** |
| 样本数 | 50k → **100k(train) + 59,400(test)** |
| 改进 | 时间标准化、金额对数化、分类字段统一字符串化。 |

🧠 **意义**：  
该集体现电商中**用户行为序列**与基本属性结合的场景。时间衍生和账户特征对检测**用户稳定性异常**至关重要。

---

### 5️⃣ col16_raw（原始电商交易集）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 16 | 14 |
| 新增 | `log_amount`, `hour_of_day`, `day_of_week` |
| 删除 | 地址文本与冗余描述字段 |
| 标签 | `Is Fraudulent` → `is_fraud` |
| 缺失率 | 少量 → **0%** |
| 样本数 | 50k+ → **100k(train) + 23,633(test)** |
| 改进 | 删除无结构字段，字段统一命名与类型转换。 |

🧠 **意义**：  
作为另一组结构化电商交易表，与 col14 对照使用，可用于测试特征派生与一致性建模的迁移效果。

---

### 6️⃣ Counterfeit Products（假货检测：卖家侧）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 27 | 16 |
| 新增 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` |
| 删除 | 文本描述、图片 URL、冗余平台统计列 |
| 标签 | `is_counterfeit` → `is_fraud` |
| 缺失率 | 原约10% → **0%** |
| 样本数 | 5,000 → **4,000(train) + 1,000(test)** |
| 改进 | 统一价格字段、规范化卖家指标（`seller_rating`, `domain_age_days`）。 |

🧠 **意义**：  
代表平台治理维度，重点分析卖家可信度、品牌异常与高风险价格行为。清洗后可支持商品级欺诈与品牌保护模型。

---

### 7️⃣ Counterfeit Transactions（假货检测：交易侧）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 特征数 | 20 | 19 |
| 新增 | `timestamp`, `hour_of_day`, `day_of_week`, `log_amount` |
| 删除 | 冗余文本列 |
| 标签 | `involves_counterfeit` → `is_fraud` |
| 缺失率 | 原<5% → **0%** |
| 样本数 | 3,000 → **2,400(train) + 600(test)** |
| 改进 | 折扣/退款类字段布尔化、时间结构化、金额标准化。 |

🧠 **意义**：  
该集从用户交易侧揭示“假货链条”的具体交易模式。配合 Counterfeit Products 数据，可联合分析**卖家端异常 + 交易端风险传播**。

---

## 🧭 Part 3. 综合总结

### ✅ 统一化成果

| 项目 | 清洗前 | 清洗后 |
|------|---------|---------|
| 时间标准化 | 各异 (`TransactionDT`, `Time`, `Date`) | 统一为 `timestamp` 并派生 `hour_of_day`, `day_of_week` |
| 金额尺度 | 单位不一、分布偏态 | 统一使用 `amount` + `log_amount` |
| 标签字段 | `Class`, `isFraud`, `Is.Fraudulent`, `is_counterfeit` 等 | 全部统一为 `is_fraud` |
| 缺失值 | 局部高达 40% | 全部清零 (填补或裁剪) |
| 样本划分 | 无固定规则 | 按 `is_fraud` 分层 80/20 划分 |
| 文件结构 | 各自独立 | 统一为 `../data/{dataset}/train`, `../data/{dataset}/test` |

### 💡 清洗价值与影响

1. **数据一致性**  
   - 时间、金额、标签字段语义完全对齐；  
   - 类型标准化后，模型接口不再依赖数据源。  

2. **质量提升**  
   - 所有数据缺失率降至 0%；  
   - 异常值经裁剪或转换后更平滑。  

3. **特征丰富性**  
   - 新增时间周期、金额对数、交易频率等可解释特征；  
   - 各数据集均保留基础可解释特征。  

4. **实验可复现性**  
   - 各数据集采用相同划分规则；  
   - 结构化保存，支持自动加载与多模型实验。  

5. **跨域扩展潜力**  
   - IEEE 与 CreditCardPCA 用于金融支付风控；  
   - col14/col16 与 Counterfeit 数据连接电商行为与假货治理；  
   - CreditCardTransaction 承接地理空间风险分析。  

